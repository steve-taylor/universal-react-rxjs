# Universal React RxJS - Browserify configuration

Browserify will automatically transpile `universal-react-rxjs`. Just set the
`NODE_ENV` environment variable to `development` or `production` when invoking
`browserify`.
